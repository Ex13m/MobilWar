services:
  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    ports: ["8080:8080"]
    volumes: ["mobilwar-data:/data"]
    environment:
      REFEREE_PIN: "1234"
volumes:
  mobilwar-data:
