PORT=8080
# Path to SQLite database (created if missing)
DB_PATH=./data/mobilwar.db
# Comma-separated allowed origins for WS (empty = allow all, dev only)
ALLOWED_ORIGINS=
# Referee PIN required for "ref" commands
REFEREE_PIN=1234
